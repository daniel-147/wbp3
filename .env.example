# Copy to .env for local development. The real .env is git-ignored.

PORT=3000
AWS_REGION=eu-west-2
DYNAMODB_TABLE=wbp3-submissions

# Local only. On EC2 leave these unset and rely on the instance role.
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
